export enum ScreenRoute {
  APP_INIT_SCREEN = 'app_init_screen',
  REQUEST_GITHUB_REPO_SCREEN = 'request_github_repo_screen',
  MAIN_SCREEN = 'main_screen',
  ISSUE_DETAIL_SCREEN = 'issue_detail_screen',
}

export enum ModalRoute {
  NATIVE_PERMISSION_REQUEST_MODAL = 'native_permission_request_modal',
}

export enum OverlayRoute {
  LOADING_OVERLAY = 'loading_overlay',
  ALERT_OVERLAY = 'alert_overlay',
}

export enum NavigationRoot {
  APP_INIT_ROOT = 'app_init_root',
  MAIN_ROOT = 'main_root'
}

export enum RouteType {
  SCREEN = 'screen',
  MODAL = 'modal',
  OVERLAY = 'overlay'
}
